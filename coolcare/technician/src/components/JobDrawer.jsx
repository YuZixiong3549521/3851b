import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogTitle } from '@/components/ui/dialog';
import {X,MapPin,CalendarDays,Clock3,Wrench} from 'lucide-react';
import {StatusBadge,PriorityBadge} from './Badges.jsx';
import {formatDate,formatTime} from '../utils/jobs.js';
export default function JobDrawer({job,onClose}){return <Dialog open onOpenChange={open=>{if(!open)onClose();}}><DialogContent showCloseButton={false} className="max-h-[90vh] overflow-y-auto sm:max-w-lg p-0" aria-describedby={undefined}><div className="drawer-inner"><div className="drawer-heading"><span className="job-id">{job.id}</span><Button variant="ghost" autoFocus className="icon-button" onClick={onClose} aria-label="Close job details"><X/></Button></div><DialogTitle>{job.customer}</DialogTitle><div className="drawer-badges"><StatusBadge value={job.status}/><PriorityBadge value={job.priority}/></div><h3>Appointment</h3><p className="cell-icon"><CalendarDays size={18}/>{formatDate(job.date)}</p><p className="cell-icon"><Clock3 size={18}/>{formatTime(job.time)}</p><p className="cell-icon"><Wrench size={18}/>{job.serviceType}</p><h3>Service address</h3><p className="cell-icon"><MapPin size={18}/>{job.address}</p><h3>Reported problem</h3><p>{job.reportedProblem||'No additional details provided.'}</p><Button variant="ghost" className="detail-button" onClick={onClose}>Back to jobs</Button></div></DialogContent></Dialog>;}
