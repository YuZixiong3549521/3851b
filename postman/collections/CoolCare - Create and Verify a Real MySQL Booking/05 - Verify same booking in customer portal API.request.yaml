$kind: http-request
url: "{{baseUrl}}/api/customer/bookings"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('HTTP success',()=>pm.expect(pm.response.code).to.be.within(200,299));
    language: text/javascript
order: 5000
